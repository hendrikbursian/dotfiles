#!/usr/bin/env zsh

rm $HOME/.config/redshift.conf 2&> /dev/null

if [[ -z $STOW_FOLDERS ]]; then
    STOW_FOLDERS="zsh,bin,asdf,nvim,tmux,z,regolith,fonts,conf,redshift"
fi

if [[ -z $DOTFILES ]]; then
    DOTFILES=$HOME/.dotfiles
fi

STOW_FOLDERS=$STOW_FOLDERS DOTFILES=$DOTFILES $DOTFILES/install

rm $HOME/.config/redshift.conf 2&> /dev/null &&
cp ./redshift/.config/redshift.conf $HOME/.config/redshift.conf 
